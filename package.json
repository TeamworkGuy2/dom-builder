{
  "name": "dom-builder",
  "version": "0.2.0",
  "description": "TypeScript DOM builder utils, simple as: new DomBuilderFactory(window.document).create('div').style('color', 'green').element;",
  "author": "TeamworkGuy2",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
  }
}
